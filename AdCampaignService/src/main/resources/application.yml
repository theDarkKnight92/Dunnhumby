spring.application.name=AdCampaignService

spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/spring-cloud-samples/config-repo

tenants:
    datasources:
        retailer1:
            connectionUrl:
            minPoolSize:
            maxPoolSize:
            connectionTimeout:

        retailer2:
            connectionUrl:
            minPoolSize:
            maxPoolSize:
            connectionTimeout: